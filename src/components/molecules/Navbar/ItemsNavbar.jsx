/* import React from "react";

const ItemsNavbar = ({ icons, items, children ,color }) => {
  return (
    <div className="flex flex-row cursor-pointer items-center hover:bg-slate-400 rounded-lg pr-1 gap-1">
        <span className={`text-2xl text-${color}`} >{icons}</span>

      <div className="flex items-center flex-col justify-between">
        {items?.map((icon, i) => (
          <div key={i}>{items[i]}</div>
        ))}
      </div>
      <p className={`line-clamp-1 text-${color} font-calibri font-medium`}>
      {children}

      </p>
    </div>
  );
};

export default ItemsNavbar; */
